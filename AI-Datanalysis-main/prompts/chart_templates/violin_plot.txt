# VIOLIN PLOT GENERATION RULES

Create exactly ONE valid Plotly violin plot and assign it to variable: result.

## Libraries
Use:
- pandas as pd
- numpy as np
- plotly.graph_objects as go
- plotly.express as px

## Data source
The datasets are already loaded as pandas DataFrames:
- DF_1, DF_2, DF_3, ...

Choose the most relevant DataFrame for the user request.

## Critical reasoning rules
1) A violin plot shows the DISTRIBUTION of values. NEVER aggregate (mean/sum/count) before plotting — pass raw row-level data directly to px.violin(). Plotly computes the kernel density automatically.
2) NEVER do `df[cols].mean()` or any cross-column aggregation before a violin plot. That reduces each column to a single scalar value, destroying the distribution.
3) For a single column distribution: `px.violin(df, y='column_name', box=True)` or `px.violin(df, x='group_col', y='value_col', box=True)`.
4) For wide-format data where MULTIPLE COLUMNS are the categories (e.g., toan, van, anh are separate columns): use `pd.melt()` to convert to long format first, then plot with `px.violin(df_long, x='variable', y='value', box=True)`.
5) If there are too many categories, sort and keep top 10 or top 20 only when necessary.
6) Use the FULL relevant dataset unless the user explicitly asks for a subset.
7) Never use markdown fences. Output raw Python code only.
8) Do not call fig.show().

## Data cleaning & Filtering rules
Before plotting and aggregating:
- create a copy: df = DF_n.copy()
- CRITICAL FILTERING: If the user explicitly asks for a subset of data (e.g., "sản phẩm là laptop", "năm 2023", "chỉ lấy vùng A"), you MUST filter the DataFrame using pandas boolean indexing (e.g., `df = df[df['product'].str.contains('laptop', case=False, na=False)]`) BEFORE applying any groupby or calculations.
- convert required numeric columns with pd.to_numeric(..., errors="coerce")
- drop rows where required x or y values are missing
- if the chart is based on aggregated values, aggregate first into a new DataFrame named agg

## Output structure
Use this generalized structure:

df = DF_n.copy()

# FILTER DATA HERE (if requested in prompt)
# df = df[df['something'] == 'target']

# optional numeric cleaning
# df["some_numeric"] = pd.to_numeric(df["some_numeric"], errors="coerce")

# optional aggregation
# agg = df.groupby(<GROUP_COL>, as_index=False)[<VALUE_COL>].mean()

# CREATE THE FIGURE HERE
fig = px.violin(df, x=<CATEGORY_COL>, y=<VALUE_COL>, box=True)

fig.update_layout(
    title=<TITLE>,
    width=900,
    height=600,
    margin=dict(l=40, r=40, t=65, b=65),
    title_font_size=20,
)

result = fig

## Wide-format data (multiple numeric columns as categories)
- If the user asks for distribution across "each category" where categories are MULTIPLE DISTINCT NUMERIC COLUMNS (e.g., each subject has its own column):
  - Use `pd.melt()` to reshape to long format: `df_long = df[numeric_cols].melt(var_name='category', value_name='value')`
  - Then plot: `px.violin(df_long, x='category', y='value', box=True)`
  - NEVER compute `df[cols].mean()` — that destroys the distribution and produces a scalar per column.
